# Najilaboule Shop (Shopify Theme)

自社の和食店ブランド「Naji la boule」のお米を販売する EC サイト用 Shopify テーマ。
ベースは Shopify 公式テーマ **Horizon 4.1.5** (Theme Store 版)。LP (https://atimot.github.io/najilaboule/) とは **色もフォントも共有しない** (2026-09-24 決定。ブラウンの暗い配色は EC には重いため、ショップ独自の「白 × 墨 × 稲穂の金」に切り替えた)。LP の構造 (店舗紹介・予約導線) も継承しない。

- ストア (管理画面ハンドル): `najilaboule` (admin.shopify.com/store/najilaboule)
- 永続ドメイン: `vuvwb5-6g.myshopify.com` ← CLI 認証・Admin API はこちら (`najilaboule.myshopify.com` は別名。OAuth コールバック不一致でエラーになる)
- 独自ドメイン (プライマリ): **`shop.najilaboule.com`** (2026-09-25 接続。Cloudflare の `najilaboule.com` ゾーンに CNAME `shop` → `shops.myshopify.com` を **DNS only** で登録。Shopify は Cloudflare プロキシ非対応なのでオレンジ雲にしない。TLS 発行 (約 5 分) 後に Shopify が自動でプライマリにし、myshopify の 2 ドメインは 301 で転送される。所有確認の TXT は不要だった。apex と www は LP (GitHub Pages) のまま)
- 公開: **2026-09-12 に Horizon を公開済み** (当初予定は 2026-10-01。ストアは引き続きパスワード保護中)

## テーマとブランチの対応 (2026-09-12 公開後)

| テーマ | ID | 状態 | 対応ブランチ |
|---|---|---|---|
| Horizon | 145592877107 | **公開中 (MAIN)** | `main` |
| najilaboule-shop/main | 143376744499 | 未公開 (旧 Dawn ベース)。GitHub 連携は 2026-09-12 に解除済み。削除候補 | なし |
| Dawn | 143036547123 | 未公開。旧。削除候補 | なし |

公開中テーマ (Horizon) の内容 = `main` ブランチ。GitHub 連携は使っておらず、反映は Shopify CLI の `push` / `pull` で行う。テーマエディタで変更したら `shopify theme pull` で `main` に取り込んでコミットする。

## デザイン決め事

デザインの正 (source of truth) はこの CLAUDE.md の「デザイン決め事」。LP の `DESIGN.md` は参照しない。

**触ってよいのは設定データだけ** (どれもテーマエディタが書き込むのと同じ JSON):

1. テーマ全体の設定 = `config/settings_data.json` の `current` (テーマエディタの「テーマ設定」)
2. セクション・ブロック単位の設定 = `templates/*.json` と `sections/*-group.json` (テーマエディタでセクションを選んだときの設定。2026-09-24 に追加)

設定できる項目・値・範囲は `config/settings_schema.json` と各セクション / ブロックの `{% schema %}` に定義されている。日本語の表示名は `locales/ja.schema.json`。

**カスタム CSS は使わない** (2026-09-24 決定)。テーマ全体 (`platform_customizations.custom_css`) もセクション単位 (テンプレート JSON 内の `custom_css`) も同じ。理由: テーマ設定の値を上書きして管理画面の設定が効かなくなる。同日に游明朝のため書体だけ例外にしたが、ライブラリの Noto Serif Japanese に切り替えて例外も無くした。テーマ設定で表現できない見た目は、テーマ設定の範囲で近いものに寄せる。**唯一の例外 (2026-09-26)**: お問い合わせフォームの「電話」欄の非表示。電話欄は `blocks/contact-form.liquid` に直書きで切り替え設定が無く (上流 Horizon の main も同じ)、Liquid を編集すると Theme Store の更新に追従できなくなるため、`templates/page.contact.json` の form セクションに `custom_css` (`#ContactForm-phone` とそのラベルを `display: none`) を置いた。テーマ設定の値は何も上書きしていない。Horizon が電話欄の切り替え設定を追加したら、それに置き換えて例外を無くす。

`sections/` `blocks/` `snippets/` `assets/` `layout/` の Liquid / CSS / JS 本体は **編集しない** (Theme Store の更新に追従できなくなるため)。

**カラーパレット (白 × 墨 × 稲穂の金。2026-09-24〜):**

| パレットキー | 値 | 役割 |
|---|---|---|
| background | #ffffff | ページ・ヘッダー・入力欄・カートの地 |
| foreground | #1f1f1f | 墨。文字、主ボタンの地、選択中バリエーションの地 |
| accent | #b8975a | 稲穂の金。セールバッジの地のみ (文字には使わない: 白地に 2.8:1 で読めない) |
| muted | #6b6b6b | 薄い文字 (価格など)、売り切れバッジの文字 |
| surface | #f5f2ec | ごく薄い生成り。副ボタンの地 |
| color1 | #1f1f1f | フッターの地 (文字色はテーマが自動で白にする) |
| color2 | #d9d4cb | 罫線・枠線 |

- ヒーロー (トップ) の見出しとボタンは写真の上なのでパレット参照ではなく白 `#ffffff` を直接指定している
- ヒーロー画像 (2026-09-24): 羽釜の中の炊きたてのご飯としゃもじ (`najila_top6.jpg`。元は `~/work/najilaboule-tmp/03_kama/najila_top6.JPG`)。元の 6000×3376 は Shopify の画像上限 (20MP) を超えるので 3840×2160 に縮小して上げた。ストアの「ファイル」で焦点を横中央・上端 (50% / 0%) に設定済み (PC の横長の切り抜きでご飯を下に寄せ、文言を暗い釜の内側に乗せるため。焦点は API では設定できず、管理画面のファイル編集で行う)。暗幕は単色 #121212 の 55% (`#1212128c`)。40% だと白い文字がご飯に重なって読めない

- フォント: **Noto Serif Japanese** (Shopify フォントライブラリ収録の Web フォント。どの端末でも同じ書体)。本文 / 小見出し / 見出し / アクセントの 4 か所すべて `noto_serif_japanese_n5` (500)。**ウェイトは 1 つだけ** (2026-09-24 決定): Shopify 配信の Noto Serif Japanese は 1 ウェイト約 2.7MB あり、見出し用に 600 を足すと初回 5.4MB になるため。見出しは大きさだけで区別する。h1〜h6 はすべて「見出し」の書体プリセットを使う (h5・h6 の既定は小見出しだが、太さを変えるときに h1〜h6 が一緒に動くよう変更済み)
- 字間: 見出し h1〜h6 は「広め」(0.03em)。本文の字間は設定が無いので標準のまま
- 見出しの大きさ (2026-09-26 決定): **h1 40 / h2 40 / h3 24 / h4 20 / h5 18 / h6 16** (px)。Horizon 既定 (72 / 48 / 32 / 24 / 18 / 16) は欧文の大型見出し向けで日本語には大きすぎるため、日本の EC の実測 (一保堂・土屋鞄: 本文 16px、商品名 18〜24px、セクション見出し 22〜30px) に寄せた。用途: h1 = ポリシーのタイトル、h2 = ヒーローのコピー・商品一覧/お問い合わせのタイトル、h3 = 商品名と商品説明の小見出し、h4 = トップの「商品」・カートのタイトル、h6 = 商品カードの商品名。選べる大きさは 16・18・20・24・32・40・48… と飛び飛び (28 や 22 は無い)。商品説明 HTML の小見出し (内容・お申し込み・出荷・送料) は `<h4>` (20px) で、商品名 (h3 24px) より一段小さい (2026-09-26 確認。商品データは 2 点とも `<h4>` で登録済み)
- 行間: 本文は「広め」(1.6)。見出し h1〜h6 は全部「ルーズ」(1.2。2026-09-26 決定。Horizon の見出し用の行間は 狭い 1.0 / 標準 1.1 / ルーズ 1.2 の 3 段階しかなく、和文の相場 1.5〜1.7 には届かないが選べる最大)。ブロック単位の「カスタム」プリセットなら 1.4 にできるが、個別に上書きせずテーマ設定で揃える方針
- 角丸はすべて 0、影なし、カードのホバー効果なし
- ボタン: primary = 墨で塗る (地 foreground・文字 background・枠 foreground)、secondary = surface 地・foreground 文字・color2 枠。ホバー色は Horizon の自動計算に任せる
- ロゴ (2026-09-24): **「Naji la boule / ナジラブール」の 2 行。アイコンは入れない** (`najilaboule-wordmark.png`、836×185、墨 #1f1f1f・背景透明)。高さは **PC 32px / スマホ 24px** (控えめにする。ナジラブールは PC で約 7px とほぼ模様扱い)。9 色アイコンはファビコンだけで使う。反転ロゴは未設定 (透明ヘッダーを使っていないので不要)。同日いったんアイコン付き (`najilaboule-logo.png`、PC 48px) にしたが取り下げ
- ファビコン (2026-09-24): 9 色の丸アイコン (`najilaboule-favicon-512.png`、512×512、地色 #231816 のまま。透明にすると白地のタブで白・黄、黒地のタブで黒・紫の丸が消える)。Horizon は 32×32 でしか出力しない
- ロゴ・ファビコンの元画像と生成スクリプトは `~/work/najilaboule-tmp/01_logo/shopify/`。ストアの「ファイル」にあり、設定値は `shopify://shop_images/<ファイル名>`。ファイル名が `icon` などで終わると Shopify が名前に識別子を足すので避ける

**2026-09-24 テーマエディタでの調整 (ユーザー設定。`shopify theme pull` で取り込み):**

- 商品ページ: ギャラリーは **1 列** (`media_columns: one`)、画像ズームなし (`zoom: false`)。「You may also like」(product-recommendations) セクションは **非表示** (`disabled: true`)
- トップの商品カード: 地を surface、枠色 color2 (枠線自体は none のまま)、カード画像の枠の不透明度 50%、商品名・価格の内側余白 8px
- フッター (footer-utilities): 上の罫線なし (`divider_thickness: 0`)。地は foreground 参照 (color1 と同じ #1f1f1f)
- ヒーローの見出しは `<br>` ではなく 2 つの `<p>` (エディタが書き換えた形。見た目は同じ)
- 同日、Claude が提案した商品ページの変更一式 (詳細列の surface 地・正方形ギャラリー・アコーディオン等) は不採用で全て戻した。再提案しない

## 商品 (2026-09-24 登録)

2026 年度販売計画 (`~/work/najilaboule-tmp/2026年度販売計画.pdf`) の 3 プランを商品として登録した (うち 1 年契約は 2026-09-26 に削除。下記) (Admin API の `productCreate`。テーマではなくストアのデータなので、このリポジトリには入らない)。

| プラン | ハンドル | 商品 ID | SKU |
|---|---|---|---|
| 初回販売 (数量限定。新之助 5kg ＋ きたりえカレー 2 個) | `shinnosuke-5kg-kitarie-curry-set` | 7982710358067 | NJ-SHIN-5KG-CURRY |
| レギュラー (12/1〜。5kg) | `shinnosuke-5kg` | 7982710554675 | NJ-SHIN-5KG |

- ストアは税込表示 (`shop.taxesIncluded = true`) なので、**商品には税込価格 (税抜 × 1.08) を入れる**。**価格・商品名・説明文の現在値はこのファイルに書かない** (2026-09-26 ユーザー指示: ストア側でコロコロ変わる値を控えに書くと古くなる。ユーザーは管理画面で直接変える)。管理画面が正。必要なら `shopify store execute` で読む (価格の変更は `productVariantsBulkUpdate`)
- 商品説明の「お申し込み・出荷」(2026-09-26 決定): 発送時期が通常 (ご注文確定後 1 週間程度) と違う商品は、説明文に (1) 予約販売である旨とお支払いはご注文時、(2) 期限つきの発送時期 (「○月上旬よりご注文順に順次発送 (○月中の発送を予定)」の形。特商法の引渡時期は期間・期限を示すのが原則で、始点だけの「順次」は避ける)、(3) 発送開始後のご注文の発送時期 を書く。ポリシー側が「各商品ページに記載の発送時期」を参照する前提。2 点とも適用済み (収穫後の出荷のため)。小見出しは `<h4>`、強調は `<strong>` と下線の span だけにし、エディタが入れる `<meta charset>` や `color: rgb(0,0,0)` は残さない
- 税率: 日本の基本税率は **8%** (Manual Tax。2026-09-26 に管理画面「設定 > 税金と関税 > 日本」で確認。都道府県は 0%、税の優先適用 (上書き) なし)。注文の消費税はこの 8% で内訳計算される (`draftOrderCalculate` で確認: 5,300 円 → 消費税 393 円)。税率用のコレクションは不要なので作らない (同日いったん作って削除した)
- **新春セール・1 年契約** (`shinnosuke-5kg-12bags-annual`、ID 7982710456371、64,800 円、SKU NJ-SHIN-5KG-12。12 袋分を一括払いする通常商品として登録していた) は **2026-09-26 にユーザー指示で削除した** (`productDelete`)。テーマ側にこの商品への参照は無かった。ポリシーの「複数回に分けてお届けする商品」の条文は同日夜にユーザーが特商法・返金ポリシーから削除 (配送ポリシーの「お届け先の変更」にだけ残る)
- セットは **数量限定** (在庫 500 で売り切れ) として売り、**申込期間 (期限) はサイトに書かない** (2026-09-26 ユーザー決定。期限を書くと「期間限定販売」として特商法 12 条の 6 の最終確認画面にも表示する義務が生じ、Basic プランでは商品名に併記するしか手が無いため。個数限定販売は該当しない)。説明文の「申込期間: 11 月 30 日まで」は同日「数量限定のため、なくなり次第販売終了となります」に差し替えた。販売計画上の初回販売の終了は管理画面で商品を非公開にして運用する
- 申込期間の開始・終了は自動化していない。2 商品とも公開中 (ストアはパスワード保護中)。12/1 公開にするなら管理画面の「公開日時を設定」か `publishablePublish` の `publishDate`
- 在庫は **追跡する** (2026-09-26 に ON。ロケーション「新潟オフィス」1 か所。初期在庫は初回販売セット 500 袋・レギュラー 4,000 袋 (ユーザー指定))。品切れ時は販売停止 (`inventoryPolicy: DENY`。在庫 0 で購入ボタンが「売り切れ」になり、商品カードに売り切れバッジが出る。Horizon 標準なのでテーマ側の変更なし)。2 商品は別々に数える (セットが売れてもレギュラーは減らない)。商品ページの「残りわずか」表示 (`product-inventory` ブロック) は入れていない。在庫数は管理画面の「商品 > 在庫」か、下記「開発フロー」の `inventorySetQuantities` で変える。送料は全国送料込み・沖縄県のみ +1,200 円 (下記「配送」。2026-09-26 設定済み)
- 画像は `~/work/najilaboule-tmp/07_rice` `08_curry` から。商品カードの縦横比 (`image_ratio: adapt`) を揃えるため、主画像は 4:3 に切り抜いた
- 旧テスト商品 3 点 (`銀座 Naji la boule の米 ― ギフト 300g / お試し 1kg / 家庭用 5kg`、2026-05-26 作成) は同日ユーザー指示で削除した。商品は上の 2 点だけ

## ページと導線 (2026-09-25 整理)

商品 3 点のショップなので使うページを絞り、使わないものは導線 (ヘッダー・フッター・メニュー) から外した。URL 自体は Shopify の仕様で残る (リンクしなければ辿り着けない)。

**使うページ:**

| ページ | URL | 中身の置き場 | 導線 |
|---|---|---|---|
| トップ | `/` | `templates/index.json` | ロゴ |
| 商品 (3 点) | `/products/<handle>` | 商品管理 | トップの商品一覧、全商品ページ |
| 全商品 | `/collections/all` | 自動生成 (コレクションは作らない) | ヘッダーメニュー「商品一覧」 |
| お問い合わせ | `/pages/contact` | ページ ID 154988380211 (テンプレート `page.contact`、フォーム付き。項目は名前・メールアドレス・コメントの 3 つ。電話欄は 2026-09-26 に custom_css で非表示。上記「デザイン決め事」の例外参照) | ヘッダーメニュー「お問い合わせ」 |
| カート | ドロワー (`/cart`) | 自動 | ヘッダーのカートアイコン |
| チェックアウト | Shopify ホスト | 設定 > チェックアウト | カート |
| 特定商取引法に基づく表記 | `/policies/legal-notice` | 設定 > ポリシー | フッターのポリシー一覧 (本文があるものが自動で並ぶ) |
| プライバシーポリシー | `/policies/privacy-policy` | 設定 > ポリシー | 同上 |
| 配送ポリシー | `/policies/shipping-policy` | 設定 > ポリシー (2026-09-25 新規) | 同上 |
| 返金ポリシー | `/policies/refund-policy` | 設定 > ポリシー (2026-09-25 新規) | 同上 |

**使わないページ・機能 (導線から外した):**

- 検索: ヘッダーの検索アイコンを OFF (`show_search: false`)。`/search` は残るがリンクしない
- コレクション: 作らない。自動生成の `/collections/all` だけ使う。`/collections` (コレクション一覧) もリンクしない
- ブログ: 作らない
- 顧客アカウント: ログインリンクを非表示 (設定 > お客様アカウント > ログインリンクの表示 OFF)。ゲスト購入のみ
- SNS リンク: フッターの social-links ブロックを削除 (Horizon 初期値の facebook.com 等のダミー URL だった)
- 国・言語セレクタ: OFF (`show_country` / `show_language: false`。もともと 1 つずつなので表示されていなかった)
- フッターメニュー (`footer`、項目「検索」のみ): Horizon のフッターが参照していないので未使用
- 利用規約: 作らない。複数回に分けてお届けする商品への言及は配送ポリシーの「お届け先の変更」にだけ残る (一括払い・途中解約不可の文は 2026-09-26 にユーザーが特商法・返金ポリシーから削除)

**メニュー (ストアのデータ。管理画面「オンラインストア > メニュー」):**

- メインメニュー (`main-menu`、ID 219581349939): 「商品一覧」(CATALOG → `/collections/all`)、「お問い合わせ」(PAGE)。初期値の「ホーム」は削除 (ロゴが兼ねる)。テーマ側はヘッダーの `_header-menu` ブロックが `main-menu` を参照しているだけ

**ポリシー (ストアのデータ。控えは `docs/policies/` に文言だけ):**

- **ポリシー・固定ページ・メニューでは特定の商品名・プラン名・数量・販売期間・年月に言及しない** (2026-09-25 ユーザー決定。商品の追加や期間限定品の入れ替えのたびに更新する手間を無くすため)。商品固有の発送時期・支払い条件・特典は商品ページの説明文に書き、ポリシー側は「各商品ページに記載」で参照する。複数回配送の商品は「複数回に分けてお届けする商品」と一般名で書く
- 書き込みは CLI: `shopify store execute --allow-mutations` で `shopPolicyUpdate` (変数は `--variable-file`)。コネクタでは書けない。**先に現在の本文を `shop { shopPolicies { type body } }` で取得し、変える部分だけ置き換える**。控えは事業者情報を `[　]` の仮置きにしているので、控えの HTML で本文全体を上書きしない (2026-09-26)
- **控えにストア側で変わる値を書かない** (2026-09-26 ユーザー指示)。事業者情報 (販売業者・運営責任者・所在地・電話番号・メールアドレス) は管理画面で記入し、控えでは `[　]` の仮置き。同じ理由で商品の価格もこのファイルに書かない
- 特商法の事業者情報 (販売業者・運営責任者・所在地・電話番号・メールアドレス) は **2026-09-26 に全部記入済み** (ユーザーが管理画面で。値はここに書かない)。管理画面のエディタで編集すると見出しに `<br>` が混入することがある (電話番号の見出しで発生、API で除去) ので、本文を API で読んだら `<h3>…<br></h3>` が無いか見る
- **引渡時期・発送時期 (2026-09-26 決定)**: 「各商品ページに記載の発送時期に発送。収穫時期に合わせてお届けする予約販売の商品は前払いで、商品ページ記載の時期からご注文順。記載が無い商品はご注文確定後 1 週間程度」(特商法・配送ポリシー同文)。それまでは「1 週間程度」が既定で異なる商品だけ商品ページ参照だったが、売っている商品が全部収穫後の出荷で既定が嘘になっていたため逆順にした。お支払い方法の注記にも予約販売は前払いと明示
- **キャンセル (2026-09-26 決定)**: 「ご注文確定後のキャンセルはお受けできません。ただし、予約販売の商品は、出荷準備に入る前であればお問い合わせフォームからのご連絡によりキャンセルを承ります。出荷準備開始後はお受けできません」(特商法・返金ポリシー同文)。同日夜にユーザーがいったん 1 文だけに削ったが、発送前の全額不返還は消費者契約法 9 条 (平均的損害を超える部分は無効) で争われうること、問い合わせ・チャージバックの火種になることを聞いて例外を復活させた。複数回配送の途中解約不可の文は該当商品が無いので戻していない
- **仮置き (要確認)**: お届け先変更はフォーム連絡 (締切なし)。決済手段の表記 (Shop Pay / Apple Pay / Google Pay) は有効な手段に合わせる。送料の記載 (全国送料込み・沖縄県のみ +1,200 円) は「配送」の設定と一致させてある (2026-09-26)
- **特商法 13 条 (前払式通信販売の承諾通知) と 12 条の 6 (最終確認画面) は 2026-09-26 に対応済み**: 引渡時期は配送方法名 (「配送」節の予約販売プロフィール)、申込期間はセットを数量限定にして非該当に (「商品」節)、承諾の一文・販売業者の住所・電話番号・引渡時期は注文確認メールの承諾欄 (「通知メール」節)。PayPal は同日ユーザーが管理画面で無効化 (特商法の記載はそのまま)。根拠と実機確認の記録は Obsidian `Notes/特商法 12 条の 6・13 条への Shopify Basic での対応 (najilaboule-shop, 2026-09 調査)`。2026-09-26 にユーザーがテスト注文で、チェックアウトの配送方法名と注文確認メールの承諾欄・インボイス欄を実注文で確認済み

## 配送 (2026-09-26 設定)

ストアのデータ (設定 > 配送と配達)。Admin API の `deliveryProfileUpdate` / `deliveryProfileCreate` で登録した (テーマではないのでこのリポジトリには入らない)。プロフィールは 2 つ: **一般プロフィール** (通常出荷の商品用。2026-09-26 夜からは商品が入っていない) と **予約販売プロフィール** (発送時期が通常と違う商品用。いまは 2 商品ともこちら)。

- 方針: **全国送料込み (商品価格に含める)。追加送料は沖縄県のみ +1,200 円。離島・一部地域は追加なし** (2026-09-26 ユーザー決定。それまでの「沖縄・離島 +1,000 円」は取り下げ)
- 一般プロフィール (ID 92507635763、ロケーショングループ 93499457587) の配送エリア:

| エリア | ゾーン ID | 対象 | 送料 (メソッド定義 ID) |
|---|---|---|---|
| 国内配送 | 352793985075 | 沖縄県を除く 46 都道府県 (JP-01〜JP-46) | 「送料込み」0 円 (637597515827。ストア初期値の「通常配送 1,000 円」を書き換えた) |
| 沖縄県 | 369024663603 | 沖縄県 (JP-47) | 「沖縄県 追加送料」1,200 円 (737677017139) |
| 国際 | 352794017843 | ストア作成時の初期値 27 か国 (「Standard」3,000 円、0〜2kg) | 未使用。海外販売はしない。触っていない。削除候補 |

- **予約販売プロフィール** (ID 97057669171、ロケーションは新潟オフィス。2026-09-26 に `deliveryProfileCreate` で作成): **配送方法の名前に発送時期を入れてある**。配送方法名はチェックアウトの最終確認画面の「配送方法」欄と注文確認メールの「配送方法」に出るので、特商法 12 条の 6 (最終確認画面の引渡時期) と 13 条 (承諾通知の引渡時期) を、Basic プランのままテーマも触らずに満たせる。ポリシーや固定ページに年月を書かない方針とも両立する (根拠と実機確認は Obsidian の調査ノート)。料金は一般プロフィールと同じ。国際ゾーンは作っていない (海外販売しない)

| エリア | ゾーン ID | 対象 | 配送方法の名前 (2026-09-26 時点) と料金 (メソッド定義 ID) |
|---|---|---|---|
| 国内配送 | 369044193331 | JP-01〜JP-46 | 「送料込み（予約販売：11 月上旬より順次発送、11 月中の発送を予定）」0 円 (737799274547) |
| 沖縄県 | 369044226099 | JP-47 | 「沖縄県 追加送料（予約販売：11 月上旬より順次発送、11 月中の発送を予定）」1,200 円 (737799307315) |

- 運用: 発送時期が変わったら予約販売プロフィールの配送方法名を書き換える (`deliveryProfileUpdate` の `methodDefinitionsToUpdate`、または管理画面)。配送方法名の発送時期は商品ページの「お申し込み・出荷」と一致させる。予約販売が終わって通常出荷になった商品は一般プロフィールに移す (管理画面の配送プロフィールで商品を移動、または `deliveryProfileUpdate` の `variantsToAssociate`)。確認は `draftOrderCalculate` の `availableShippingRates` (2026-09-26 に東京 0 円・沖縄 1,200 円で新しい名前が出ることを確認済み)
- 追加送料は注文 1 件につき 1 回 (チェックアウト時に加算)
- 送料への課税 (`shop.taxShipping`) は OFF のまま。沖縄の 1,200 円を税込 (10%) として扱うなら「設定 > 税金と関税 > 配送料に税金を課す」を ON にする (**未決定**。API では変更できない)。基本税率が 8% なので、ON にするときは「日本」の「税の優先適用」で配送に 10% を設定する
- 同じ内容を配送ポリシー・特商法の「送料」と、商品説明 3 点の「送料」に書いてある (2026-09-26 に 1,200 円へ更新済み)
- 離島は Shopify 標準の配送エリアでは判定できない (都道府県単位のみ)。追加送料を取らないことにしたので対応不要

## ロケーションと住所 (2026-09-26 確認)

ストアのデータ (テーマではないのでこのリポジトリには入らない)。住所の設定は 3 か所あり、役割が違う:

| 設定 | 役割 | 入れる住所 |
|---|---|---|
| 設定 > ストアの詳細 > 請求先住所 (`shop.billingAddress`) | Shopify からの請求書用。テーマの `shop.address` もこれ (Horizon は表示していない) | 会社の住所 |
| 設定 > ロケーション (`locations`) | 発送元。配送プロフィールの発送元、梱包明細 (納品書) の `shop_address` に出る | 発送場所の住所 |
| 特商法の [住所] (ポリシー) | 販売業者の所在地 (法定記載) | 会社の住所 |

- ロケーションは 1 つ (ID 75946819635。名前は「新潟オフィス」(2026-09-26 にユーザーが管理画面で変更。それまでは住所から自動生成の「1-6-4」)、`fulfillsOnlineOrders: true`)。住所は **発送場所として正しい** (2026-09-26 ユーザー確認。値はここに書かない)
- 請求先住所 (`shop.billingAddress`) は **2026-09-26 時点で会社の住所** (株式会社源屋創助・中央区日本橋茅場町。ユーザーが管理画面で設定。値はここに書かない)。ストアの電話番号 (`shop.phone`) は空。注文確認メールの承諾欄で `shop.address` / `shop.phone` を使うなら電話番号もここに入れる
- ロケーションの住所は店頭受取を使わない限りストアには表示されない。発送元が増えない限りロケーションは 1 つのまま
- 配送業者の送り状の差出人 (返品時の返送先) は業者側のシステムで設定する。Shopify とは連動しない

## 通知メール (2026-09-26)

ストアのデータ (設定 > 通知 > お客様への通知)。Admin API では読み書きできず、管理画面のエディタ (CodeMirror) でしか編集できない。

- **注文確認メールが適格簡易請求書を兼ねる** (2026-09-26 ユーザー決定・反映済み)。フッター `<table class="row footer">` の直前に「適格簡易請求書」欄を挿入した。控えは `docs/notifications/注文確認メール インボイス欄.liquid`。Shopify 既定の本文 (約 3,850 行) はそれ以外いじっていない
- 発行事業者: 株式会社源屋創助、登録番号: T8010401107641 (2026-09-26 ユーザー提供)
- **注文確認メールが特商法 13 条の承諾通知も兼ねる** (2026-09-26 反映)。インボイス欄の直前 (インボイス欄の先頭の comment タグの手前) に「ご注文の承諾について」欄を挿入した。控えは `docs/notifications/注文確認メール 承諾欄.liquid`。中身: 承諾の一文 (支払い待ちでなければ「代金 {合計} を {注文日} に受領いたしました」、`has_pending_payment` で分岐)・販売業者 (直書き)・所在地 `shop.address` (郵便番号・都道府県・市区町村・住所 1・2。都道府県は API では「Tōkyō」だがメールでは「東京都」と出る)・電話番号 `shop.phone`・メールアドレス `shop.email`・お届け時期 (配送方法名 `shipping_method.title` の全角括弧の中。括弧が無ければ「ご注文確定後 1 週間程度で発送いたします」)。住所・電話・メールは設定 > ストアの詳細の値が出るので、変わったら設定を直すだけ (メールは直さない)。Liquid のコメント内に `comment` タグを書くと入れ子になって壊れるので控えのコメントにも書いていない
- 欄の中身: 発行事業者・登録番号・取引年月日・注文番号・税率ごとの税込合計と消費税額 (各明細の `tax_lines` で 8% / 10% / 対象外に振り分け)・軽減税率対象の注記 (全商品が 8% なら「すべて軽減税率対象」、混在なら品目名を列挙)。適格簡易請求書の必須 5 項目を満たす。送料は課税していないので税率別集計の外に別行で表示
- 編集手順 (Chrome): 差出人メールアドレス (`info@najilaboule.com`) の認証が済んでいないとエディタがロックされる (2026-09-26 に認証済み)。本文は CodeMirror 6 で、`document.querySelector('.cm-editor .cm-content').cmView.view` から `state.doc` の読み取りと `dispatch({changes})` での書き換えができる。書き換えると上部に「未保存の変更」バーが出るので「保存」を押す。Claude in Chrome の JS 出力は `=` を含む HTML を返すと「Cookie/query string」として遮断されるので、本文を読むときはページ内に `<article><pre>` を作って `get_page_text` で読む
- **実注文で確認済み** (2026-09-26 にユーザーが Shopify ペイメントのテストモードでテスト注文): 注文確認メールにインボイス欄 (8% 側に振り分け) と承諾欄 (受領金額・受領日・住所・電話・お届け時期に配送方法名の括弧内) が正しく出た
- 編集の注意 (2026-09-26): `dispatch` の直後は「保存」ボタンがまだ無いことがある。数秒待ってから `button` のテキストが「保存」のものを `click()` する。保存後のトースト「通知テンプレートが保存されました」で確認し、プレビュー (`/email_templates/order_confirmation/preview`) の iframe の `innerText` で描画を読む
- 送料に課税するよう変えたら、この欄で送料を 10% の集計に足す (控えのコメント参照)

## 開発フロー

- 設定を変えたら: `shopify theme push --store vuvwb5-6g.myshopify.com --theme 145592877107 --allow-live --only <変えたファイル>` (例: `--only config/settings_data.json --only templates/index.json`)。`--allow-live` が無いと公開中テーマへの確認プロンプトで止まる (非対話だと無言で待つ)
- テーマエディタで変えたら: `shopify theme pull --store vuvwb5-6g.myshopify.com --theme 145592877107` で取り込んでコミット
- プレビュー: `shopify theme dev --store vuvwb5-6g.myshopify.com` (http://127.0.0.1:9292) または管理画面のテーマ プレビュー (ストアはパスワード保護中)
- push 先は公開中テーマ (Horizon) なので、`shopify theme push` の前に必ず `pull` して差分を確認する。`--only` で対象ファイルを絞る。pull は未コミットの変更があると確認で止まるので、先にコミットしてから `pull --only <同じファイル>` し、`git diff` でエディタ側の変更を見る
- ストア操作の CLI 認証 (`shopify store auth`) のスコープ: products / publications / files / legal_policies / online_store_navigation / online_store_pages / shipping / draft_orders / inventory の read・write、locations の read (2026-09-26 時点。shipping・draft_orders・read_locations・inventory は同日追加)。別セッションが `store auth` を再実行するとスコープが入れ替わる (2026-09-26 に read_inventory が消えていた) ので、スコープは差分でなく毎回全部を列挙する
- 在庫数の変更は `inventorySetQuantities` (`name: "available"`、`reason: "received"` / `"correction"` など)。CLI が使う API バージョンでは各項目に `changeFromQuantity` (変更前の現在値。`inventoryItem.inventoryLevels.quantities(names: ["available"])` で取る) が必須で、ミューテーションのフィールドに `@idempotent(key: "<UUID>")` を付けないと拒否される (`ignoreCompareQuantity` は無い)。追跡の ON/OFF は `inventoryItemUpdate(id, input: { tracked })`。在庫の読み取りには read_inventory が要る
- 税額・送料の動作確認は `draftOrderCalculate` (注文を作らずに計算だけする。住所の `provinceCode` は `JP-13` 形式) で行う。チェックアウト画面で確認する必要はない
- 実注文でしか確認できないもの (通知メールの文面、チェックアウトの配送方法名、税の振り分け) は、Shopify ペイメントの **テストモード** (設定 > 決済 > Shopify ペイメント > 管理) を ON にしてテストカード `4242 4242 4242 4242` (有効期限は未来、CVC 任意) でお客様として注文する (2026-09-26 実施)。注文・メール・在庫は本番同様に動くので、終わったら注文をキャンセルして「在庫を戻す」、テストモードを **OFF** に戻す (ON のままだと本物の注文が通らない)。API でのテスト注文はしない (orders のスコープを付けていない)
- コミットしたら `main` に取り込み (fast-forward) GitHub に push するところまで進める (2026-09-24 ユーザー承認。毎回の確認は不要)
